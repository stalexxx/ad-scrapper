ktor {
    deployment {
        port = 8080
        watch = [ com.stalex.main ]
    }

    application {
        modules = [ com.stalex.main ]
    }
}

jwt {
    domain = "https://jwt-provider-domain/"
    audience = "jwt-audience"
    realm = "ktor sample app"
}